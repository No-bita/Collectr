{
  "name": "collectrr",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm --prefix v2 run dev",
    "test": "npm --prefix v2 test",
    "test:offline": "npm --prefix v2 run test:offline",
    "db:init:local": "npm --prefix v2 run db:init:local",
    "db:migrate:local": "npm --prefix v2 run db:migrate:local",
    "scan:secrets": "node scripts/scan-secrets.js"
  }
}
